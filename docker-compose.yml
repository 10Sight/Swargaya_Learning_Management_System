version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    environment:
      - DB_HOST=host.docker.internal # Default to host for Windows if DB is local
      # Override other env vars here if needed
    volumes:
      - ./server:/app
      - /app/node_modules

  admin:
    build: ./admin
    ports:
      - "5173:5173"
    env_file:
      - ./admin/.env
    volumes:
      - ./admin:/app
      - /app/node_modules
    depends_on:
      - server
